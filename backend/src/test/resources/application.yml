spring:
  datasource:
    url: jdbc:h2:mem:srm;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  jwt:
    secret: test-zhuatech-srm-secret-must-be-at-least-thirty-two-bytes
    expiration: PT1H
  cors:
    allowed-origins: http://localhost:5173
